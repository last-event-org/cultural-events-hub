<!DOCTYPE html>
<html lang="{{ i18n.locale }}">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ i18n.t('messages.profile_type_title') }}</title>
  @vite(['resources/css/app.css', 'resources/js/app.js'])
</head>

<body>
  @include("pages/partials/header")
  <main class="bg-gray-100 flex items-center justify-center min-h-screen ">

    <div class="bg-white p-8 rounded shadow-md w-full max-w-3xl">
        <h2 class="text-2xl font-bold mb-6 text-center text-gray-700">
            {{-- Bienvenue {{ user.firstname }}! Que voulez vous faire ? --}}
            {{ i18n.t('messages.profile_type_welcome', { userFirstname: user.firstname }) }}
        </h2>
        <div class="flex gap-4 justify-center">
            <a href={{ route('events.index') }}>
              <button class="p-4 bg-teal-400 rounded-md">
                {{ i18n.t('messages.profile_type_buy') }}
              </button>
            </a>
            <button onclick="displayVendorDataForm()" class="p-4 bg-teal-400 rounded-md">
                {{ i18n.t('messages.profile_type_sell') }}
            </button>
        </div>

      <form id="vendorDataForm" action="{{ route('auth.register.update-profile-type') }}" method="POST" class="space-y-4 hidden">
        {{ csrfField() }}

        @error('E_INVALID_CREDENTIALS')

        {{ $message }}

        @end

        <div>
            <label for="company_name" class="block text-gray-600">
                {{ i18n.t('messages.profile_type_company') }}
            </label>
            <input type="text" id="company_name" name="company_name" class="mt-1 block w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm
   focus:ring-slate-500 focus:border-slate-700 sm:text-sm" value="{{old('company_name') || '' }}">
            @inputError('company_name')
            <div style="color: red;">{{ $messages.join(', ') }}</div>
            @end
        </div>

        <div>
            <label for="vat_number" class="block text-gray-600">
                {{ i18n.t('messages.profile_type_vat') }}
            </label>
            <input type="text" id="vat_number" name="vat_number" class="mt-1 block w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm
   focus:ring-slate-500 focus:border-slate-700 sm:text-sm" value="{{old('vat_number') || '' }}">
            @inputError('vat_number')
            <div style="color: red;">{{ $messages.join(', ') }}</div>
            @end
        </div>

        <div>
            <div class="block text-gray-800 text-xl font-semibold my-4">
                <h2>{{ i18n.t('messages.profile_type_billingAddr') }}</h2>
            </div>
            <div class="flex flex-col gap-2 my-4">
                <label for="street" class="block text-gray-600">
                    {{ i18n.t('messages.profile_type_street') }}
                </label>
                <input type="text" id="street" name="street" class="mt-1 block w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm
         focus:ring-slate-500 focus:border-slate-700 sm:text-sm" value="{{old('street') || '' }}">
                @inputError('street')
                <div style="color: red;">{{ $messages.join(', ') }}</div>
                @end

                <label for="number" class="block text-gray-600">
                    {{ i18n.t('messages.profile_type_nb') }}
                </label>
                <input type="text" id="number" name="number" class="mt-1 block w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm
         focus:ring-slate-500 focus:border-slate-700 sm:text-sm" value="{{old('number') || '' }}">
                @inputError('number')
                <div style="color: red;">{{ $messages.join(', ') }}</div>
                @end

                <label for="zip_code" class="block text-gray-600">
                    {{ i18n.t('messages.profile_type_zip') }}
                </label>
                <input type="text" id="zip_code" name="zip_code" class="mt-1 block w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm
         focus:ring-slate-500 focus:border-slate-700 sm:text-sm" value="{{old('zip_code') || '' }}">
                @inputError('zip_code')
                <div style="color: red;">{{ $messages.join(', ') }}</div>
                @end

                <label for="city" class="block text-gray-600">
                    {{ i18n.t('messages.profile_type_city') }}
                </label>
                <input type="text" id="city" name="city" class="mt-1 block w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm
         focus:ring-slate-500 focus:border-slate-700 sm:text-sm" value="{{old('city') || '' }}">
                @inputError('city')
                <div style="color: red;">{{ $messages.join(', ') }}</div>
                @end

                <label for="country" class="block text-gray-600">
                    {{ i18n.t('messages.profile_type_country') }}
                </label>
                <input type="text" id="country" name="country" value="Belgique" class="mt-1 block w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm
         focus:ring-slate-500 focus:border-slate-700 sm:text-sm" value="{{old('country') || '' }}">
                @inputError('country')
                <div style="color: red;">{{ $messages.join(', ') }}</div>
                @end
            </div>
        </div>

        <button type="submit" class="w-full bg-gray-800 text-white py-2 px-4 rounded-md hover:bg-gray-700 
                  focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-slate-500">
                  {{ i18n.t('messages.profile_type_createAccount') }}
        </button>

      </form>

    </div>
  </main>
  @include("pages/partials/footer")
  <script src="/resources/js/register_vendor_data.js"></script>
</body>

</html>