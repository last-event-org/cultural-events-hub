<!DOCTYPE html>
<html lang="fr">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Next Event Formulaire d'enregistrement</title>
  @vite(['resources/css/app.css', 'resources/js/app.js'])
</head>

<body>
  @include("pages/partials/header")
  <main class="bg-gray-100 flex  justify-center min-h-screen">
    
    <div class="bg-white p-8 mt-20 mb-20 rounded shadow-md w-full max-w-md">
      <h2 class="text-2xl font-bold mb-6 text-center text-gray-700">
        Enregistrement
      </h2>
      <form action="" method="POST" class="space-y-4">
        {{ csrfField() }}
        <div>
          <label for="first_name" class="block text-gray-600">
            Nom
          </label>
          <input type="text" id="first_name" name="first_name" class="mt-1 block w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm
           focus:ring-yellow-500 focus:border-yellow-700 sm:text-sm" placeholder="Dupont" value="{{old('first_name') || '' }}">
        </div>
        <div>
          <label for="last_name" class="block text-gray-600">
            Prénom
          </label>
          <input type="text" id="last-name" name="last_name" class="mt-1 block w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm
           focus:ring-slate-500 focus:border-slate-700 sm:text-sm" placeholder="Jean" value="{{old('last_name') || '' }}">
        </div>

        <div>
          <label for="email" class="block text-gray-600">
            Email
          </label>
          <input type="email" id="email" name="email" class="mt-1 block w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm
           focus:ring-slate-500 focus:border-slate-700 sm:text-sm" placeholder="vous@exemple.com" value="{{ old('email') || '' }}">
           
           @inputError('email')
              @each(message in $messages)
                <p> {{ message }} </p>
              @end
            @end
        </div>

        <div>
          <label for="password" class="block text-gray-600">
            Mot de passe
          </label>
          <input type="password" id="password" name="password" class="mt-1 block w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm
          focus:ring-slate-500 focus:border-slate-700 sm:text-sm" placeholder="********">
          @inputError('password')
            @each(message in $messages)
              <p> {{ message }} </p>
            @end
          @end
          @flashMessage('password')
            <p> {{ $message }} </p>
          @end
        </div>

        <div>
          <label for="password_confirmation" class="block text-gray-600">
            Confirmez le mot de passe
          </label>
        <input type="password" id="password_confirmation" name="password_confirmation" class="mt-1 block w-full px-4 py-2
         border border-gray-300 rounded-md shadow-sm focus:ring-slate-500 focus:border-slate-700 sm:text-sm" placeholder="********">
         @inputError('password_confirmation')
          @each(message in $messages)
            <p> {{ message }} </p>
          @end
        @end
        </div>

        <div>
          <button type="submit" class="mt-10 w-full bg-yellow-500 text-gray-800 py-2 px-4 rounded-md hover:bg-yellow-400 
          focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-slate-500">
          Accepter
          </button>
        </div>
        
      </form>
    </div>
  </main>

  @include("pages/partials/footer")

</body>
</html>