<!DOCTYPE html>
<html lang="{{ i18n.locale }}">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ i18n.t('messages.orders_title') }}</title>
  @vite(['resources/css/app.css', 'resources/js/app.js'])
</head>

<body class="bg-slate-50">

  @include("pages/partials/header")

  <main class= "container mb-10 mx-auto relative">
     
    <div class="flex justify-center  gap-10  mx-auto my-6 py-10"> 
      <article class="bg-slate-50 border-gray-300 rounded-lg w-full">
        <h2 class="text-4xl font-semibold tracking-tight text-gray-700">
          {{ i18n.t('messages.orders_title') }}
        </h2>
        <div class="flex flex-col justify-center items-center mb-4">
            @if(orders.length === 0)
                <p>{{ i18n.t('messages.orders_noOrders') }}</p>
            @else
             @each(order in orders)
                <div class="flex flex-col lg:flex-row w-full lg:border-2 bg-slate-400 border-yellow-500 rounded-md">
                  {{ i18n.t('messages.orders_order') }}: {{order.id}}
                  {{ i18n.t('messages.orders_purchaseDate') }}: {{order.purchaseDate}}
                    @each(orderLine in order.orderLineId)
                    {{ i18n.t('messages.orders_qty') }}: {{orderLine.qty}}
                    {{ i18n.t('messages.orders_event') }}: {{orderLine.price.event.title}}
                    {{ i18n.t('messages.orders_date') }}: {{orderLine.price.event.eventStart}}
                    {{ i18n.t('messages.orders_ticketType') }}: {{orderLine.price.description}}
                    {{ i18n.t('messages.orders_price') }}: {{orderLine.price.discountedPrice}}
                    @end
                </div>
             @end
     
            @end
        </div>
        </article>
    </div>
    
  </main>


  @include("pages/partials/footer")

</body>
</html>