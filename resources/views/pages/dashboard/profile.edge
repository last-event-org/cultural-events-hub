<!DOCTYPE html>
<html lang="fr">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Next Event Page de Profil</title>
  @vite(['resources/css/app.css', 'resources/js/app.js'])
</head>

<body class="bg-slate-50 ">
  @include("pages/partials/header")
  {{-- {{route('auth.profile.edit')}} --}}
<main class="flex flex-col items-center min-h-screen gap-10">
  <section class=" bg-white text-slate-600 flex flex-col border rounded-md shadow mt-20">
      <div class="justify-center flex">
        <h2 class="relative z-10 font-bold text-xl my-4">Profil 
            <hr class="absolute -z-10 w-full h-2 bottom-1 left-0  bg-yellow-500">
        </h2>
      </div>
    
      <div class="w-full md:w-[600px]">
        <div class="flex flex-col lg:flex-row lg:items-center">
          <div class="flex self-center lg:self-start w-32 h-32 lg:w-40 lg:h-40  shadow m-4 p-4  rounded-md">
            <img src="/svg/profile_picture_dummy.svg" alt="profile picture dummy">
          </div>
          <div class="flex flex-col gap-2 mb-2 lg:my-6  mx-4">
            <div class="flex gap-2">
              <p class="font-bold">Nom:</p>
              <p>Dupont</p>
              
            </div>
            <div class="flex gap-2">
              <p class="font-bold">Prénom:</p>
              <p>Jean</p>
            </div>
            <div class="flex gap-2">
              <p class="font-bold">Date de naissance:</p>
              <p>26/07/1986</p>
            </div>
            <div class="flex gap-2">
              <p class="font-bold">Email:</p>
              <p>jean.dupont@caramail.com</p>
            </div>
            <div class="flex gap-2">
              <p class="font-bold">Adresse:</p>
              <p>Rue de Mulhouse 36, 4020 Liège - Belgique</p>
            </div>          
          </div>   
      </div>

      <div class="flex flex-col gap-2 mx-4">
        <div class="flex gap-2">
          <p class="font-bold">Nom de société:</p>
          <p>Les films du plat pays</p>
        </div>
        <div class="flex gap-2">
          <p class="font-bold">N° de TVA:</p>
          <p>BE26 3789 5214 7896</p>
        </div>
      </div>

      <div class="flex justify-center my-8">
        <div class="flex bg-indigo-700 text-white py-1 px-2 rounded-md font-bold">
          <a href="{{route('auth.profile.edit')}} ">
            Éditer le profil
          </a> 
        </div>
      </div>  
    </div>
  </section>

  <section>
    <div class="min-w-full md:w-[600px] bg-white text-slate-600 flex flex-col border rounded-md shadow px-4 ">
      <div class="justify-center flex">
        <h2 class="relative z-10 font-bold text-xl my-4">Whishlist 
            <hr class="absolute -z-10 w-full h-2 bottom-1 left-0  bg-yellow-500">
        </h2>
      </div>
      <div class="flex flex-col gap-4 pb-8 ">
        {{-- @if wishlist is empty --}}
        {{-- <p>Pas d'évènement dans votre wishlist pour le moment.</p> --}}
        {{-- @else --}}
        @!component('components/dashboard/mini-event')
        @!component('components/dashboard/mini-event')
        @!component('components/dashboard/mini-event')
        @!component('components/dashboard/mini-event')
        @!component('components/dashboard/mini-event')
      </div>
    </div>
  </section>
  <section>
    <div class="w-full md:w-[600px] bg-white flex flex-col border rounded-md shadow px-4">
      
      <div class="justify-center flex">
        <h2 class="relative z-10 font-bold text-xl my-4">Vendeurs favoris
            <hr class="absolute -z-10 w-full h-2 bottom-1 left-0  bg-yellow-500">
        </h2>
      </div>
        <div class="flex flex-col gap-4 pb-8">
          @!component('components/dashboard/vendor')
          @!component('components/dashboard/vendor')
          @!component('components/dashboard/vendor')
        </div>
      </div>
    </div>
  </section>
</main>
@include("pages/partials/footer")

</body>
</html>